const BASE_URL = "/api/v1";

const COINS = {
  bitcoin: "bitcoin",
  matic: "matic-network",
  ethereum: "ethereum",
};

const CURRENCY = "usd";

module.exports = { BASE_URL, COINS, CURRENCY };
